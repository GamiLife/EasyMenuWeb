import styled from '@emotion/styled';
import { Container } from '@gamiui/standard';

export const AllSocialNetworks = styled(Container)`
    display: flex;
    
    button{
        height: 30px;
        padding-inline: 8px;
        width: 46px;
    }
`;